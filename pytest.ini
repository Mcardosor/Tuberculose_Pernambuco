[pytest]
testpaths = tests
addopts = -q --tb=short
filterwarnings =
    ignore:.*ScriptRunContext.*:UserWarning
    ignore:.*No runtime found.*
markers =
    lento: exercita a base inteira em muitas combinações (desmarque com -m "not lento")
